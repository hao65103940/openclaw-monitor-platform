# 监控平台更新日志 - 2026-03-07

## 🎯 本次更新

### 1. 配置管理页面（Configs）

**功能**：
- ✅ 自动扫描 `/root/.openclaw/agents/*/agent/` 目录
- ✅ 显示所有 `.md` 和 `.json` 配置文件
- ✅ 查看、编辑、保存功能
- ✅ 真实文件操作（非模拟数据）
- ✅ 文件类型图标（📝 Markdown / 📊 JSON）

**API 端点**：
- `GET /api/agents/config/list` - 获取配置列表
- `GET /api/agents/:id/config/:file` - 读取文件
- `PUT /api/agents/:id/config/:file` - 保存文件

**访问地址**：http://localhost:3000/configs

---

### 2. 执行流程追踪（Trace）

**功能**：
- ✅ 执行流程时间线（垂直布局）
- ✅ 实时状态显示（🟢 运行中 / ✅ 已完成 / ❌ 失败）
- ✅ 会话类型标识（直接/群组/定时任务/子 Agent）
- ✅ 性能指标（Token、耗时、时间戳）
- ✅ 详情弹窗
- ✅ 过滤器（全部/活跃/已完成）
- ✅ Agent 协作统计
- ✅ **渠道维度分析**（新增）
- ✅ 自动刷新（30 秒）

**渠道维度分析**（新增）：
- 📝 飞书渠道会话统计
- 💼 企业微信渠道会话统计
- 🖥️ Control-UI 渠道会话统计
- ⏰ 定时任务统计
- 🤖 子 Agent 统计

**API 端点**：
- `GET /api/trace/flow` - 获取执行流程
- `GET /api/analytics/channels` - 渠道维度分析（新增）

**访问地址**：http://localhost:3000/trace

---

### 3. 全局数据刷新优化

**状态管理**（useAgentStore）：
- ✅ 添加 `refreshInterval` 配置（默认 30 秒）
- ✅ 添加 `autoRefresh` 开关（默认开启）
- ✅ 添加 `setRefreshInterval` 方法
- ✅ 添加 `toggleAutoRefresh` 方法

**使用方式**：
```typescript
const { refreshAll, autoRefresh, toggleAutoRefresh } = useAgentStore();

// 手动刷新
await refreshAll();

// 切换自动刷新
toggleAutoRefresh();

// 设置刷新间隔
setRefreshInterval(60000); // 60 秒
```

---

## 📊 渠道维度分析详情

### 渠道识别规则

| 会话 Key 格式 | 识别渠道 | 说明 |
|--------------|---------|------|
| `agent:main:main` | control-ui | Control-UI 直接会话 |
| `agent:main:feishu:group:xxx` | feishu | 飞书群组会话 |
| `agent:main:wecom:group:xxx` | wecom | 企业微信群组会话 |
| `agent:main:cron:xxx` | cron | 定时任务 |
| `agent:main:subagent:xxx` | subagent | 子 Agent |

### 统计指标

每个渠道显示：
- 📊 会话次数
- 💰 Token 消耗总量
- 📈 平均 Token 消耗
- ⏱️ 总耗时
- ⏱️ 平均耗时
- 🟢 活跃会话数
- 🤖 涉及 Agent 列表

---

## 🎨 界面布局

### Trace 页面

```
┌─────────────────────────────────────────────────────────────┐
│ [💬 总会话数] [🟢 活跃] [📊 总 Token] [⏱️ 平均耗时]        │
├─────────────────────────────────────────────────────────────┤
│ 🔍 执行流程追踪  [全部] [🟢 活跃] [✅ 已完成]  [🔄 刷新]    │
├─────────────────────────────────────────────────────────────┤
│ 执行流程时间线：                                            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 1️⃣ 主 Agent  🟢 running  [control-ui]                │  │
│  │    📊 108K tokens  ⏱️ 2.5m  🕐 03-07 18:17          │  │
│  └──────────────────────────────────────────────────────┘  │
│  ↓                                                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ 2️⃣ 需求分析师  ✅ completed  [subagent]              │  │
│  │    📊 15K tokens  ⏱️ 1m  🕐 03-07 17:45             │  │
│  └──────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 📱 渠道维度分析：                                           │
│  ┌──────────────┬──────────────┬──────────────┐            │
│  │ 📝 飞书      │ 💼 企微      │ 🖥️ Control   │            │
│  │ 1 次会话     │ 2 次会话     │ 1 次会话      │            │
│  │ 24K tokens   │ 98K tokens   │ 66K tokens   │            │
│  │ 🟢 0 活跃    │ 🟢 0 活跃    │ 🟢 1 活跃    │            │
│  └──────────────┴──────────────┴──────────────┘            │
├─────────────────────────────────────────────────────────────┤
│ 🤝 Agent 协作统计：                                         │
│  🤖 主 Agent：5 次 · 250K tokens · 54.8%                    │
│  🤖 需求分析师：1 次 · 15K tokens · 3.3%                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 技术实现

### 后端（server-real.js）

```javascript
// 渠道分析 API
app.get('/api/analytics/channels', (req, res) => {
  const sessions = runOpenClawCommand(['sessions', '--json']);
  
  // 按渠道分组统计
  const channelStats = sessions.reduce((acc, session) => {
    const channel = parseChannel(session.key);
    acc[channel].count += 1;
    acc[channel].tokens += session.totalTokens;
    // ...
  }, {});
  
  res.json({ channels: channelStats });
});
```

### 前端（Trace.tsx）

```typescript
// 加载所有数据
async function loadAllData() {
  const [traceResponse, channelResponse] = await Promise.all([
    api.get('/trace/flow'),
    api.get('/analytics/channels'),
  ]);
  
  setFlow(traceResponse.data.flow);
  setChannelStats(channelResponse.data.channels);
}

// 自动刷新
useEffect(() => {
  loadAllData();
  const interval = setInterval(loadAllData, 30000);
  return () => clearInterval(interval);
}, []);
```

---

## 📝 相关文件

### 新增文件
- `/root/.openclaw/monitor-platform/README-TRACE.md` - Trace 页面文档
- `/root/.openclaw/monitor-platform/UPDATE-2026-03-07.md` - 本文档

### 修改文件
- `/root/.openclaw/monitor-platform/server-real.js` - 添加渠道分析 API
- `/root/.openclaw/monitor-platform/src/pages/Trace.tsx` - 添加渠道维度 UI
- `/root/.openclaw/monitor-platform/src/pages/Configs.tsx` - 真实文件操作
- `/root/.openclaw/monitor-platform/src/App.tsx` - 添加 Trace 路由
- `/root/.openclaw/monitor-platform/src/store/useAgentStore.ts` - 刷新优化
- `/root/.openclaw/monitor-platform/README.md` - 更新功能说明

---

## 🎉 访问地址

| 页面 | 地址 | 说明 |
|------|------|------|
| **仪表盘** | http://localhost:3000/ | Agent 列表和统计 |
| **执行链路** | http://localhost:3000/trace | 执行流程 + 渠道分析 |
| **性能分析** | http://localhost:3000/analytics | Token 和模型分析 |
| **配置管理** | http://localhost:3000/configs | Agent 配置编辑 |

---

## 🚀 下一步优化

1. **子 Agent 链路追踪** - 明确显示父子 Agent 关系
2. **任务分解可视化** - 展示主 Agent 如何分解任务
3. **实时日志流** - 显示 Agent 执行的实时日志
4. **甘特图视图** - 展示并行执行的任务
5. **WebSocket 实时推送** - 替代轮询，实现真正的实时更新

---

**更新时间**：2026-03-07 18:30  
**版本**：v1.2.0
